//Create an array with 5 numbers. Ask the user for the length of the array you want to print. Alert the user of the array from 0 to their input


var nums = ["5", "6", "7", "8", "9"];

var len = prompt("enter length of array you want to print");

for (var i = 0; i < len; i++) {
    console.log(nums[i])
}

